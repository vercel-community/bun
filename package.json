{
  "name": "bun-vercel",
  "devDependencies": {
    "bun-types": "^1.0.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "bun build ./src/bootstrap.ts --compile --minify --outfile ./.vercel/output/functions/App.func/bootstrap"
  }
}